import { PixelRatio } from "react-native";

// export function normaliseFontSize(size: number, width: number) {
// 	const scale = width / 375;
// 	return Math.round(PixelRatio.roundToNearestPixel(size * scale));
// }
